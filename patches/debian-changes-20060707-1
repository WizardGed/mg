Description: Upstream changes introduced in version 20060707-1
 This patch has been created by dpkg-source during the package build.
 Here's the last changelog entry, hopefully it gives details on why
 those changes were made:
 .
 mg (20060707-1) unstable; urgency=low
 .
   * Initial release (Closes: #375408)
 .
 The person named in the Author field signed this changelog entry.
Author: Trent Buck <trentbuck@gmail.com>
Bug-Debian: http://bugs.debian.org/375408

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: http://bugs.debian.org/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: <YYYY-MM-DD>

--- mg-20060707.orig/Makefile.in
+++ mg-20060707/Makefile.in
@@ -11,7 +11,7 @@ prefix=		/usr/local
 bindir=		$(prefix)/bin
 libdir=		$(prefix)/lib
 includedir=	$(prefix)/include
-mandir=		$(prefix)/man
+mandir=		$(prefix)/share/man
 
 
 CC=		gcc
--- mg-20060707.orig/INSTALL
+++ mg-20060707/INSTALL
@@ -10,7 +10,7 @@ If you want to add compilerflags do it l
     make CFLAGS="-O2 -fstack-protector"
 
 
-If you don't like the installation pathes you can change them like this:
+If you don't like the installation paths you can change them like this:
 
     make install prefix=/usr mandir=/usr/share/man
 
